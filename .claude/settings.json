{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | bash -c '{ read -r f; [[ \"$f\" == *.py ]] || exit 0; python -m py_compile \"$f\"; }'",
            "timeout": 10,
            "statusMessage": "Checking Python syntax..."
          }
        ]
      }
    ]
  }
}
